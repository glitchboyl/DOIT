import 'package:flutter/material.dart';

class NotePublishingPage extends StatelessWidget {
  const NotePublishingPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
